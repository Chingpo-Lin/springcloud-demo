
server:
  port: 7000

spring:
  application:
    name: cloud-user-service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://120.24.94.91:3306/cloud_user?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: cannot tell u
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848

mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
